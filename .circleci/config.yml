version: 2.1

executors:
  jdk-postgres:
    parameters:
      pg_img:
        type: string
      pg_user:
        type: string
    docker:
      - image: cimg/openjdk:17.0
      - image: << parameters.pg_img >>
        user: << parameters.pg_user >>
        environment:
          POSTGRES_USER: postgres
          POSTGRES_DB: circle_test
          POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
          TZ: "/usr/share/zoneinfo/Asia/Tokyo"

jobs:
  fun-with-postgresql:
    executor:
      name: jdk-postgres
      pg_img: cimg/postgres:14.7
      pg_user: postgres
    resource_class: medium
    steps:
      - checkout
workflows:
  main:
    jobs:
      - fun-with-postgresql
